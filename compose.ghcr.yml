name: linksift

services:
  linksift:
    image: ghcr.io/loveisbl1nd/linksift:${LINKSIFT_VERSION:-latest}
    container_name: linksift
    ports:
      - "127.0.0.1:8899:8899"
    volumes:
      - linksift-downloads:/app/downloads
    restart: unless-stopped

volumes:
  linksift-downloads:
